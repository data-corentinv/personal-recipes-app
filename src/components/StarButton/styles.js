import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  starButton: {
    position: 'absolute',
    top: 8,
    right: 8,
    padding: 8,
    zIndex: 1,
  },
  starIcon: {
    width: 24,
    height: 24,
  },
});

export default styles; 